package edu.iu.habahram;

public class Problems {
    /**
     * finds the indices of the two elements in
     * the numbers array that add up to target.
     * If no such elements found returns {-1, -1}.
     * If there is only one element in the numbers array it should
     * throw
     * @param numbers
     * @param target
     * @return the two indices as an array
     */
    public  int[] problem1(int[] numbers, int target) {
        int[] indices = {-1, -1};
        //TODO
        return indices;
    }

    public  boolean problem2(int x) {
        boolean result = true;
        //TODO
        return result;
    }

    public  String problem3(String[] strings) {
        String answer = "";

        //TODO
        return answer;
    }
}
